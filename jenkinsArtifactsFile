pipeline{
    agent { label 'ansible'}
    stages{
        stage('SCM-Checkout'){
            steps{

                sh label: '', script: 'ansible sonar -m ping'
                sh label: '', script: 'ansible arti -m ping'
            }
        }
    }
}